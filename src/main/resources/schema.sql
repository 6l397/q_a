CREATE TABLE sandbox_items (
                               id BIGINT PRIMARY KEY,
                               name VARCHAR(255) NOT NULL,
                               category VARCHAR(255) NOT NULL
);